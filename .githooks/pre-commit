#!/bin/bash
# Validate conventional commit format on staged changes
# Actual version bump happens in commit-msg hook (which has access to the message)

# Nothing to validate at pre-commit stage — just ensure hook chain works
exit 0
